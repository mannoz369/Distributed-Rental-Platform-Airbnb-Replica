apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airbnb-replica

resources:
  - namespace.yaml
  - configmap.yaml
  - redpanda.yaml
  - services.yaml
  - deployments.yaml
  - hpa.yaml
