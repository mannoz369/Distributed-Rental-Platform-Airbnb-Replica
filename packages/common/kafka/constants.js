const TOPICS = {
  BOOKING_EVENTS: "booking.events",
};

const EVENT_TYPES = {
  BOOKING_CREATED: "booking.created",
  BOOKING_CANCELLED: "booking.cancelled",
};

module.exports = {
  EVENT_TYPES,
  TOPICS,
};
